{
  "schemaVersion": "1.2",
  "description": "${description}",
  "parameters": {},
  "runtimeConfig": {
    "aws:runShellScript": {
      "properties": [
        {
          "id": "0.aws:runShellScript",
          "runCommand": ["${command}"]
        }
      ]
    }
  }
}